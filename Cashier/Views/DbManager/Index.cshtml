@{
    ViewData["Title"] = "数据库管理";
    var dbSets = ViewBag.DbSets as List<string> ?? new List<string>();
}

<h2>选择要管理的 DbSet</h2>

<ul>
    @foreach (var set in dbSets)
    {
        <li><a asp-action="Compare" asp-route-dbSetName="@set">@set</a></li>
    }
</ul>